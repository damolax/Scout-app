SCOUT v10.42 TYPECHECK HOTFIX
=============================

Fixed:
1. app/api/import-jobs/export/route.ts now explicitly types the CSV dynamic-header reducer as Set<string>.
2. JSON row_data is normalized to Record<string, unknown> before dynamic key access.
3. The keep-open launcher reports PARTIAL SUCCESS only after the log contains GITHUB PUSH VERIFIED.
4. A TypeScript exit code 2 before the push is now correctly reported as STOPPED.

The failed attempt shown in scout-v10-42-deploy-20260725-020332.log did not update GitHub because it stopped during npm run typecheck, before git add/commit/push.
