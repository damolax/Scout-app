{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm config set registry https://registry.npmjs.org/ && npm ci --legacy-peer-deps --registry=https://registry.npmjs.org/",
  "buildCommand": "NEXT_TELEMETRY_DISABLED=1 npm run build",
  "outputDirectory": ".next"
}
