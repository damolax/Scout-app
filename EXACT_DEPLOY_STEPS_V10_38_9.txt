SCOUT v10.38.9 — EXACT CODE DEPLOYMENT

This update changes the Replies page only and does not require another Supabase SQL migration.
Your existing v10.38.8 follow-up SQL remains valid.

1. Extract the ZIP so this exact folder exists:
   C:\Users\Administrator\Downloads\SCOUT_GOOGLE_VERIFICATION_V10_38_9_REPLIES_SIMPLIFIED

2. Confirm package.json is directly inside that folder.

3. Open Git Bash and paste exactly:
   bash ~/Downloads/SCOUT_GOOGLE_VERIFICATION_V10_38_9_REPLIES_SIMPLIFIED/DEPLOY_V10_38_9_GOOGLE_VERIFICATION_GIT_BASH.sh

4. Wait for:
   DEPLOYMENT PUSH COMPLETE
   Vercel will deploy the updated main branch automatically.

5. In Vercel, open the latest production deployment and confirm it is Ready.

6. Open Scout > Replies and confirm:
   - no Sync replies card or /operations link
   - four summary cards
   - visible verification-mode notice
   - search, Show, Template, and Sender filters
   - Real replies table
   - three closed sections: Automatic messages, Delivery problems, Other stored signals
   - stored database errors appear in a red box instead of showing a false empty state

Do not run git init or git push --force. The included script creates a backup branch first.
