SCOUT V10.35 — EXACT SAFE ROLLOUT

IMPORTANT
- Back up the current working deployment before changing anything.
- Run only RUN_THIS_SQL_FIRST_V10_35.sql for this release.
- The SQL is additive, but it must be applied before the new code is deployed.

1. CHOOSE THE REAL PRODUCTION IDENTITY
   Decide the exact production URL and real support email, for example:
   https://scout.YOUR_DOMAIN
   support@YOUR_DOMAIN

2. SET VERCEL ENVIRONMENT VARIABLES
   Keep all existing variables. Add or confirm:

   NEXT_PUBLIC_APP_URL=https://scout.YOUR_DOMAIN
   GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID
   NEXT_PUBLIC_GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID
   GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_OAUTH_CLIENT_SECRET
   NEXT_PUBLIC_SCOUT_BRAND_NAME=Scout by We Are Creative Builders
   NEXT_PUBLIC_SUPPORT_EMAIL=support@YOUR_DOMAIN

   GMAIL_SEND_ENABLED=true
   GMAIL_REPLY_SYNC_ENABLED=false
   GMAIL_NATIVE_SIGNATURE_SYNC_ENABLED=false
   DELIVERABILITY_CENTER_ENABLED=true
   SENDER_HEALTH_ENFORCEMENT_ENABLED=true
   PLACEMENT_TESTS_ENABLED=true
   TEAM_PAGINATION_ENABLED=true
   ACCOUNT_DELETION_ENABLED=true
   SCOUT_MAX_ACTIVE_SENDER_LANES=8

3. CONFIGURE GOOGLE CLOUD
   User type: External
   Publishing project: the production Scout project
   Authorized JavaScript origin:
   https://scout.YOUR_DOMAIN

   Authorized redirect URI:
   https://scout.YOUR_DOMAIN/api/gmail/oauth/callback

   Declare only the scopes used by this release:
   openid
   email
   profile
   https://www.googleapis.com/auth/gmail.send

   Remove gmail.readonly and gmail.settings.basic from the first verification submission.

4. RUN THE DATABASE MIGRATION
   Open Supabase > SQL Editor.
   Copy all of RUN_THIS_SQL_FIRST_V10_35.sql.
   Run it once.
   Confirm it completes successfully.

5. DEPLOY THE CODE
   Put scout-app-v10-35-safe-sending-google-verification.zip in Windows Downloads.
   Open Git Bash and run:

   cd ~/Downloads
   unzip -p scout-app-v10-35-safe-sending-google-verification.zip '*/DEPLOY_V10_35_SAFE_GIT_BASH.sh' > DEPLOY_V10_35_SAFE_GIT_BASH.sh
   chmod +x DEPLOY_V10_35_SAFE_GIT_BASH.sh
   bash DEPLOY_V10_35_SAFE_GIT_BASH.sh

6. WAIT FOR VERCEL
   Confirm the newest deployment is Ready.
   Open these public pages while signed out:
   /
   /privacy
   /terms
   /data-deletion
   /contact
   /google-data-use

7. RUN ONE CONTROLLED TEST
   - Sign in as admin.
   - Open Settings and confirm the workspace timezone.
   - Connect one Gmail account through the new send-only flow.
   - Save one Scout signature.
   - Send one message to a test inbox you control.
   - Confirm one signature, one Sent record, and the Sent today count.
   - Send a six-recipient test with two Gmail accounts before any larger run.

8. CHECK SIMPLE SAFETY BEHAVIOR
   - New/recovering senders should not use Fast automatically.
   - Normal sending should need no extra workflow.
   - Fast should be available only for a healthy sender.
   - Existing replies should remain visible.
   - New replies should be handled in Gmail while reply sync is disabled.

9. SUBMIT GOOGLE VERIFICATION
   Open /google-verification as the main Scout admin.
   Complete the checklist and record the unlisted English demo video.
   Publish the OAuth app to Production, then submit from Google's Verification Center.

10. DO NOT ENABLE YET
   Keep these false until the appropriate advanced Google authorization is approved:
   GMAIL_REPLY_SYNC_ENABLED=false
   GMAIL_NATIVE_SIGNATURE_SYNC_ENABLED=false
