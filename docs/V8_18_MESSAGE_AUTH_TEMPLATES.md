# v8.18 Message Auth + Templates

- Added dedicated `/templates` page for message categories and template library.
- Message page is now only for selecting ready contacts, template mode, sender mode, and schedules.
- Gmail sender connection moved to Settings.
- Added settings-based Gmail OAuth redirect `/settings`.
- Added rotate/specific template controls.
- Added rotate/specific sender controls.
- Saved schedules now store the selected template/sender mode in `raw`.
- Old `/email-scout` still redirects to `/message`.
